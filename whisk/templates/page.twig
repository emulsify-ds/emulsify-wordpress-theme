{# Generated child-theme example override. It extends the parent-only page
   fallback through @emulsify-tpl to avoid recursively extending itself. #}
{% extends '@emulsify-tpl/page.twig' %}

{% block content %}
	<div class="whisk-page">
		{{ parent() }}
	</div>
{% endblock %}
